# Frequencies — Claude Context

## Mirrored entry: pitch-circle → "The Intrinsic Structure of Chords"

`diagrams/pitch-circle.html` is also published as a standalone entry on the personal blog at `../signals/intrinsic-structure-of-chords/index.html` (served as `entropicbloom.com/intrinsic-structure-of-chords/`, linked from `_posts/2026-05-25-intrinsic-structure-of-chords.md`).

The signals copy is a snapshot, not a symlink. When `diagrams/pitch-circle.html` changes in a way worth publishing, refresh the copy:

```
cp diagrams/pitch-circle.html ../signals/intrinsic-structure-of-chords/index.html
```

Then commit in the signals repo. No build step on either side.
